org.gradle.caching=true
group=io.ballerina
version=0.1.0-SNAPSHOT

ballerinaLangVersion=2201.13.2
picocliVersion=4.0.1
gsonVersion=2.10.1
testngVersion=7.10.2

# Packages the :native jar into a ballerina/tool_discover bala (bal-tool/build.gradle). Pinned to the
# same version ballerina-platform/openapi-tools uses; double-check compatibility with
# ballerinaLangVersion above before relying on it — not verified against a newer pin in this environment.
ballerinaGradlePluginVersion=2.3.0

